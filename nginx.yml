---
- name: Install Nginx
  hosts: localhost
  become: true
  tasks:
   - name: Update apt cache
     ansible.builtin.apt:
       update_cache: yes
     when: ansible_os_family == "Debian"

   - name: Install Nginx package
     ansible.builtin.package:
       name: nginx
       state: present
